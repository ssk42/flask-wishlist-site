import SwiftUI
import WishlistKit

// Stub — fleshed out in Task 9.
struct MyListView: View {
    let session: Session
    var body: some View { NavigationStack { Text("My List").navigationTitle("My List") } }
}
