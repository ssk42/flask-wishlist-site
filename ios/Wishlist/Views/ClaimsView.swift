import SwiftUI
import WishlistKit

// Stub — fleshed out in Task 9.
struct ClaimsView: View {
    let client: APIClient
    var body: some View { NavigationStack { Text("Claims").navigationTitle("Claims") } }
}
