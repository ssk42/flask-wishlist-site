import SwiftUI
import WishlistKit

// Stub — fleshed out in Task 10.
struct ActivityView: View {
    let client: APIClient
    var body: some View { NavigationStack { Text("Activity").navigationTitle("Activity") } }
}
